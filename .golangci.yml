version: "2"

linters:
  default: standard
  enable:
    - errcheck
    - govet
    - ineffassign
    - misspell
    - revive
    - staticcheck
    - unused
    - wsl_v5

formatters:
  enable:
    - gofmt
    - goimports
